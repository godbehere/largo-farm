export default interface Product {
  product_id: number;
  product_name: string;
  category: string;
  description: string;
  price: number;
  unit: string;
  quantity: number;
  thumbnail: string;
}
